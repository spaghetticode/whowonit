= render 'form'

%h2 Tracked Auctions

%table.auctions{:cellpadding => 0, :cellspacing => 0}
  %thead
    %tr
      %th{:colspan => 2} Auction
      %th End time
      %th Price
      %th Seller
      %th Buyer
  %tbody
    = render @auctions